[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-e2e-pipeline"
version = "0.1.0"
description = "End-to-end tile pipeline — mint, validate, score, store, search, dedup"
license = {text = "MIT"}
requires-python = ">=3.8"
